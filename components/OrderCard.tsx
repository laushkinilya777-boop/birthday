import Link from 'next/link';
import { CalendarDays, MapPin, MessageCircle } from 'lucide-react';
export type OrderCardData = { id:string; title:string; description:string; category:string; price:number; city:string; district?:string|null; urgency:string; status:string; createdAt:Date; date?:Date|null; author:{name:string|null;email:string}; _count?:{applications:number} };
const labels:Record<string,string>={OPEN:'Открыт',IN_PROGRESS:'В работе',COMPLETED:'Завершён',CANCELLED:'Отменён'};
export function StatusBadge({value}:{value:string}) { const colors:Record<string,string>={OPEN:'bg-blue-50 text-blue-700',IN_PROGRESS:'bg-amber-50 text-amber-700',COMPLETED:'bg-emerald-50 text-emerald-700',CANCELLED:'bg-slate-100 text-slate-600'}; return <span className={`rounded-full px-2.5 py-1 text-xs font-bold ${colors[value]||colors.OPEN}`}>{labels[value]||value}</span>; }
export default function OrderCard({order}:{order:OrderCardData}) { return <Link href={`/orders/${order.id}`} className="group flex h-full flex-col rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition duration-200 hover:-translate-y-1 hover:border-slate-300 hover:shadow-lg"><div className="flex items-start justify-between gap-3"><span className="rounded-lg bg-orange-50 px-2.5 py-1 text-xs font-bold text-orange-700">{order.category}</span><StatusBadge value={order.status}/></div><h2 className="mt-4 text-lg font-bold leading-snug text-slate-900 group-hover:text-blue-700">{order.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-500">{order.description}</p><div className="mt-5 space-y-2 text-sm text-slate-500"><div className="flex items-center gap-2"><MapPin size={16}/>{order.city}{order.district ? `, ${order.district}` : ''}</div><div className="flex items-center gap-2"><CalendarDays size={16}/>{order.date ? new Date(order.date).toLocaleDateString('ru-RU') : order.urgency === 'URGENT' ? 'Срочно' : 'По договорённости'}</div></div><div className="mt-auto flex items-end justify-between border-t border-slate-100 pt-4"><div><div className="text-xs text-slate-400">Оплата</div><strong className="text-xl text-slate-900">{order.price.toLocaleString('ru-RU')} ₸</strong></div><div className="text-right text-xs text-slate-500"><div className="flex items-center justify-end gap-1"><MessageCircle size={14}/>{order._count?.applications || 0} откликов</div><div className="mt-1">{order.author.name || order.author.email}</div></div></div></Link>; }
