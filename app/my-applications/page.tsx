export const dynamic='force-dynamic';
import { getServerSession } from 'next-auth/next'; import { authOptions } from '../../lib/auth'; import { prisma } from '../../lib/prisma'; import Link from 'next/link'; import { StatusBadge } from '../../components/OrderCard';
export default async function MyApplications(){const s=await getServerSession(authOptions as any);const id=(s as any)?.user?.id;if(!id)return null;const apps=await prisma.application.findMany({where:{userId:id},orderBy:{createdAt:'desc'},include:{order:{include:{author:true}}}});return <div className="mx-auto max-w-4xl px-4 py-10 sm:px-6"><p className="text-sm font-bold text-orange-600">Личный кабинет</p><h1 className="mt-1 text-3xl font-black">Мои отклики</h1><div className="mt-7 space-y-3">{apps.map(a=><Link href={`/orders/${a.orderId}`} key={a.id} className="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold">{a.order.title}</h2><p className="mt-1 text-sm text-slate-500">{a.order.city} · {a.order.price.toLocaleString('ru-RU')} ₸</p><p className="mt-2 text-sm text-slate-600">{a.message || 'Без комментария'}</p></div><StatusBadge value={a.status==='ACCEPTED'?'IN_PROGRESS':a.order.status}/></Link>)}</div>{!apps.length&&<div className="mt-7 rounded-2xl border border-dashed bg-white p-10 text-center">Вы пока ни на что не откликались.<br/><Link href="/orders" className="mt-3 inline-block font-bold text-blue-700">Найти задачу</Link></div>}</div>}
