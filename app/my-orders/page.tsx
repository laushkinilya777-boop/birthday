export const dynamic='force-dynamic';
import { getServerSession } from 'next-auth/next'; import { authOptions } from '../../lib/auth'; import { prisma } from '../../lib/prisma'; import Link from 'next/link'; import OrderCard from '../../components/OrderCard';
export default async function MyOrders(){const s=await getServerSession(authOptions as any);const id=(s as any)?.user?.id;if(!id)return null;const orders=await prisma.order.findMany({where:{authorId:id},orderBy:{createdAt:'desc'},include:{author:true,_count:{select:{applications:true}}}});return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><p className="text-sm font-bold text-orange-600">Личный кабинет</p><h1 className="mt-1 text-3xl font-black">Мои заказы</h1><p className="mt-2 text-slate-500">Управляйте созданными задачами и выбирайте исполнителей.</p><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{orders.map(o=><OrderCard key={o.id} order={o}/>)}</div>{!orders.length&&<div className="mt-7 rounded-2xl border border-dashed bg-white p-10 text-center">Вы ещё не создали ни одного заказа.<br/><Link href="/orders/create" className="mt-3 inline-block font-bold text-blue-700">Создать заказ</Link></div>}</div>}
