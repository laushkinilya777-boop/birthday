'use client';

import Image from 'next/image'; import Link from 'next/link'; import { signIn } from 'next-auth/react'; import { useForm } from 'react-hook-form'; import { useState } from 'react';
export default function SignInPage(){const{register,handleSubmit}=useForm<{email:string;password:string}>();const[submitting,setSubmitting]=useState(false);const onSubmit=async(data:{email:string;password:string})=>{setSubmitting(true);await signIn('credentials',{email:data.email,password:data.password,callbackUrl:'/orders'});};const input='mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-orange-400';return <div className="relative grid min-h-[calc(100vh-64px)] place-items-center overflow-hidden px-4 py-10"><div className="absolute -left-32 top-0 h-72 w-72 rounded-full bg-orange-100 blur-3xl"/><form onSubmit={handleSubmit(onSubmit)} className="relative w-full max-w-md rounded-3xl border border-slate-200 bg-white p-7 shadow-xl shadow-slate-200/60"><Image src="/brand/logo-banner.png" alt="DoWorkHere" width={418} height={218} className="h-16 w-32 object-contain" priority/><h1 className="mt-5 text-2xl font-black">С возвращением</h1><p className="mt-2 text-sm text-slate-500">Войдите, чтобы создавать задачи и откликаться на них.</p><label className="mt-6 block text-sm font-bold">Email<input required type="email" {...register('email')} className={input}/></label><label className="mt-4 block text-sm font-bold">Пароль<input required type="password" {...register('password')} className={input}/></label><button disabled={submitting} className="mt-6 w-full rounded-xl bg-slate-900 py-3.5 font-bold text-white hover:bg-slate-700 disabled:opacity-60">{submitting?'Входим…':'Войти в DWH'}</button><p className="mt-5 text-center text-sm text-slate-500">Нет аккаунта? <Link href="/auth/signup" className="font-bold text-blue-700">Регистрация</Link></p></form></div>}
