'use client';
import { useState } from 'react'; import { Star } from 'lucide-react';
export default function ReviewForm({orderId,onDone}:{orderId:string;onDone?:()=>void}){const[rating,setRating]=useState(5),[comment,setComment]=useState(''),[loading,setLoading]=useState(false),[state,setState]=useState<'idle'|'success'|'error'>('idle'),[error,setError]=useState('');const submit=async()=>{setLoading(true);setState('idle');try{const r=await fetch(`/api/orders/${orderId}/complete`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({review:{rating,comment}})});const j=await r.json();if(!r.ok)throw new Error(j.error);setState('success');onDone?.()}catch(e){setState('error');setError(e instanceof Error?e.message:'Не удалось сохранить отзыв')}finally{setLoading(false)}};if(state==='success')return <div className="rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">Спасибо! Отзыв сохранён.</div>;return <div className="max-w-xl"><div className="flex gap-1">{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setRating(n)} aria-label={`Оценка ${n}`} className="rounded-lg p-1"><Star size={27} className={n<=rating?'fill-amber-400 text-amber-400':'text-slate-200'}/></button>)}</div><textarea value={comment} onChange={e=>setComment(e.target.value)} placeholder="Расскажите, как исполнитель справился с задачей" className="mt-4 h-24 w-full resize-none rounded-xl border border-slate-200 p-3 text-sm outline-none focus:border-orange-400"/>{state==='error'&&<p className="mt-2 text-sm text-red-700">{error}</p>}<button disabled={loading} onClick={submit} className="mt-3 rounded-xl bg-slate-900 px-4 py-3 text-sm font-bold text-white hover:bg-slate-700 disabled:opacity-60">{loading?'Сохраняем…':'Опубликовать отзыв'}</button></div>}
